// loads all of the existing tags to a list
// this is need for the drop-down menu

module.exports = function (objectrepository) 
{
    return function (req, res, next) 
    {
        next();
    };
};